# PhoneLink Ubuntu — dépendances Python
#
# PyGObject (GTK4 + Adwaita) DOIT être installé via apt, pas pip :
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1
#
# Aucune dépendance PyPI supplémentaire n'est requise.
# Tous les outils système sont appelés via subprocess :
#   bluetoothctl, pactl, wpctl, pavucontrol, adb, scrcpy, xdg-open
#
# Pour les installer :
#   sudo apt install bluez pavucontrol adb scrcpy
